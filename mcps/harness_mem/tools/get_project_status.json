{
  "name": "get_project_status",
  "description": "Return active project, memory counts, and slash-native next-step triage hints without requiring CLI status.",
  "inputSchema": {
    "type": "object",
    "properties": {
      "project_name": {
        "type": "string",
        "description": "Project name (defaults to active project when omitted)"
      }
    }
  }
}
