{
  "name": "reject_memory_entry",
  "description": "Reject a pending memory entry.",
  "inputSchema": {
    "type": "object",
    "properties": {
      "entry_id": {
        "type": "string",
        "description": "Memory entry ID to reject"
      }
    },
    "required": [
      "entry_id"
    ]
  }
}
