{
  "name": "confirm_memory_entry",
  "description": "Confirm a pending memory entry.",
  "inputSchema": {
    "type": "object",
    "properties": {
      "entry_id": {
        "type": "string",
        "description": "Memory entry ID to confirm"
      }
    },
    "required": [
      "entry_id"
    ]
  }
}