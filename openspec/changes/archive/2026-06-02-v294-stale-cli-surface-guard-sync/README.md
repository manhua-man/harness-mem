# v294-stale-cli-surface-guard-sync

OpenSpec change for harness-mem v2.9.4.

Theme: keep the stale CLI surface guardrails aligned with the shipped
maintenance-only `config` and `integration` command surface.
