# v2910-worker-mode-truth-sync

OpenSpec change for harness-mem v2.9.10.

Theme: align `worker.mode` docs and roadmap summaries with the shipped
`off|on` config truth, and guard that wording with a focused regression test.
