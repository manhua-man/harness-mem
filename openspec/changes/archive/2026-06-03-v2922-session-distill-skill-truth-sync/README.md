# v2922-session-distill-skill-truth-sync

OpenSpec change for harness-mem v2.9.22.

Theme: align `tools/session-distill/SKILL.md` and the plugin distill summary
with the shipped `auto_review_candidates(apply=true)` review surface.
