# v2912-distill-mode-truth-sync

OpenSpec change for harness-mem v2.9.12.

Theme: align `distill.mode` docs and roadmap summaries with the shipped
`defer_to_agent|inline|worker` config truth, and guard that wording with a
focused regression test.
