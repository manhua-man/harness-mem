# v2915-wake-entrypoint-truth-sync

OpenSpec change for harness-mem v2.9.15.

Theme: align the repo-local `/hm:wake` command doc and `harness-mem` skill
guidance with the shipped MCP `wake` surface, and guard that wording with a
focused regression test.
