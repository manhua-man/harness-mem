# v2917-distill-auto-review-entrypoint-truth-sync

OpenSpec change for harness-mem v2.9.17.

Theme: align `/hm:distill`, repo-local skill guidance, and the MCP distill
example with the shipped `auto_review_candidates(project_name=<project>,
apply=true)` review surface.
