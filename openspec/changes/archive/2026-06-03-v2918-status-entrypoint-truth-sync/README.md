# v2918-status-entrypoint-truth-sync

OpenSpec change for harness-mem v2.9.18.

Theme: align `/hm:status` guidance and the MCP status example with the shipped
`get_project_status(project_name=<project>)` triage surface.
