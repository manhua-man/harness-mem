# Fresh-Home Write-Path Embedding Fail-Fast

This archived change tracks the v2.9.56 runtime fix that keeps interactive
write-path MCP tools responsive on a fresh isolated home by skipping cold-cache
embedding downloads instead of attempting them inline.
