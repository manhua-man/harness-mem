# v295-shell-completion-maintenance-truth

OpenSpec change for harness-mem v2.9.5.

Theme: keep `--completion` aligned with the shipped maintenance-only CLI
surface, including `config` and `integration`.
