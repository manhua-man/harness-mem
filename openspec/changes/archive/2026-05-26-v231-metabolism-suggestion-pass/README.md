# v231-metabolism-suggestion-pass

Generate audited metabolism suggestion candidates (merge / stale / supersede) on top of the v2.3.0 signals + replay window foundation; deferred upgrades: content-based token trim, weak-link signal application.
