# v2919-best-practices-auto-review-truth-sync

OpenSpec change for harness-mem v2.9.19.

Theme: align `docs/best-practices.md` with the shipped
`auto_review_candidates(project_name=<project>, apply=true)` distill review
surface.
