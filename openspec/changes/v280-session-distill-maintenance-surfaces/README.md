# v280-session-distill-maintenance-surfaces

OpenSpec change for harness-mem v2.8.0.

Theme: formalize `/hm:mark` / `/hm:prune` and the session-distill maintenance
surface as versioned workflow truth.
