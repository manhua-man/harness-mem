[pytest]
testpaths = tests
markers =
    api: API tests
    benchmark: benchmark and evaluation tests
    cli: CLI tests
    integration: integration tests
    loop_harness: 4-role memory loop end-to-end scenarios
    mcp: MCP tests
    storage: storage tests
