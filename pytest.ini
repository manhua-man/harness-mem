[pytest]
testpaths = tests
markers =
    cli: CLI behavior and workflow tests
    integration: end-to-end or multi-component integration tests
    api: REST API tests
    mcp: MCP tool and protocol tests
    storage: storage and index tests
    benchmark: benchmark tooling and benchmark-adjacent tests
