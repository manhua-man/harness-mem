[pytest]
testpaths = tests
norecursedirs = tests/session-distill
markers =
    api: API tests
    benchmark: benchmark and evaluation tests
    cli: CLI tests
    integration: integration tests
    mcp: MCP tests
    storage: storage tests
