# Acceptance Checklist

- Active generation becomes visible only after `manifest.json` is committed.
- Interrupted generation sidecars are not the active manifest generation.
- Source fingerprint drift is detectable.
- Result rows include sidecar size, fallback metadata, and `claim_readiness`.
- The report states that runtime index fabric implementation remains a later
  v4.0.x slice.
