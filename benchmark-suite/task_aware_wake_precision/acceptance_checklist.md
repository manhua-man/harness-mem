# Acceptance Checklist

- Result rows include `precision_at_k`, `budget_tokens`, and
  `budget_used_tokens`.
- `wake_packet` includes inclusion/omission rationale and budget trace.
- Default task-aware wake excludes cold/archive unless deep recall is explicit.
