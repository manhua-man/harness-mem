# Claim Promotion Pack Acceptance Checklist

- [ ] Every result row has `benchmark_id=claim_promotion_pack`.
- [ ] Every claim id in the policy set appears exactly once.
- [ ] `storage_v2_speedup`, `default_reranker_hyde`, and
  `code_memory_token_runtime` stay `blocked`.
- [ ] `blocking` is a string array.
- [ ] The report says bounded local readiness is not a broad public claim.

