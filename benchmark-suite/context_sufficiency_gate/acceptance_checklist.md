# Acceptance Checklist

- Result rows include `sufficiency_status`, `safe_to_answer`, and
  `missing_evidence_count`.
- Insufficient cases must recommend an abstention or evidence expansion action.
- Iterative retrieval must stay within the configured round limit.
