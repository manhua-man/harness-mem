# Acceptance Checklist

- `run_manifest.json`, `dataset.manifest.json`, `summary.csv`, `report.md`,
  `results/`, and `notes/` are present.
- Result rows include checksum, canonical row count, and claim readiness.
- `claim_readiness.ready` stays false unless non-smoke performance evidence is
  attached.
