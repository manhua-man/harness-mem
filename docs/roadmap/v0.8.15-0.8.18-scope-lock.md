# v0.8.15–v0.8.18 scope-lock

Mode: **HOLD SCOPE** — one concern per PR/version.

## In scope (max 3)

1. Release / MCP export consistency (PR0 cleanup + v0.8.15).
2. KNN product truth: vec0 KNN on filtered hybrid path when sqlite-vec is available (v0.8.16).
3. vec0 lifecycle + regression tests through v0.8.18.

## Deferred

See [defer.md](./defer.md). This sprint does **not** include golden expansion, admission preflight, RRF tuning, or bulk MCP router sync.

## Acceptance one-liners

- `code/mcps/harness_mem/tools/*.json` matches `tool_specs` (export test).
- Hybrid vector search uses vec0 KNN with `extra_where` post-filter when sqlite-vec is installed; batch cosine is fallback only.
- Upgraded stores: `maintenance rebuild-vector-index` or lazy backfill clears HM-204 vec0 lag.
- Targeted regression tests green; package version **0.8.18**.