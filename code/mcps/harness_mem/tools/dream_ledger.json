{
  "name": "dream_ledger",
  "description": "Return the latest background Dream status for a project, or one run by id. This is read-only and never changes memory.",
  "inputSchema": {
    "type": "object",
    "properties": {
      "project_name": {
        "type": "string",
        "description": "Project name (defaults to the active project when omitted)."
      },
      "run_id": {
        "type": "string",
        "description": "Optional DreamRun id for drilldown."
      }
    }
  }
}
