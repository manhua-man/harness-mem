{
  "name": "list_candidates",
  "description": "List temporary knowledge candidates that still exist in the current processing workspace. Candidates are not long-term memory.",
  "inputSchema": {
    "type": "object",
    "properties": {
      "project_name": {
        "type": "string",
        "description": "Project name"
      },
      "status": {
        "type": "string",
        "enum": [
          "pending",
          "deferred",
          "conflict",
          "rejected",
          "assimilated"
        ],
        "description": "Temporary candidate status.",
        "default": "pending"
      },
      "limit": {
        "type": "integer",
        "description": "Optional candidate count; omitted returns all matching candidates"
      }
    },
    "required": [
      "project_name"
    ]
  }
}
